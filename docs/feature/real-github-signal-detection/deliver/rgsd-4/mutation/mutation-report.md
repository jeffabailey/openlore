# RGSD-4 Mutation Report — `DocsPresentAndSubstantial` pure detector

- **Tool**: cargo-mutants 25.3.1 (no `--in-place`; mutants tested in copied tree)
- **Command**: `cargo mutants -p scraper-domain --file crates/scraper-domain/src/detect.rs`
- **Gated surface (RGSD-4, target ≥ 80%)**: `is_substantial_readme`, the `>=`
  boundary against `README_SUBSTANTIAL_BYTES`, and
  `detect_docs_present_and_substantial` (the README-substantial-OR-docs disjunction).
- **Killed by**: `scraper-domain`'s in-crate proptests (no new test needed).

## Whole-file tally (context)

45 mutants tested in ~1m48s: **38 caught, 5 unviable, 1 missed, 1 timeout.**
The 1 missed (`197:43 && → || in is_semver_tag`) and 1 timeout
(`210:15 += → *= in matches_semver_core_at`) are both **RGSD-3 semver code**,
outside the RGSD-4 gated surface (task: "do NOT chase pre-covered RGSD-1/2/3
arm mutants").

## RGSD-4 detector kill rate — the gated surface

| Line | Mutant | Outcome | Killed by |
|------|--------|---------|-----------|
| 144:5  | `is_substantial_readme -> true`  | CAUGHT | `readme_at_threshold_fires_but_one_below_does_not` (below-threshold + no docs must not fire); `docs_present_fires_only_on_the_readme_or_docs_disjunction` |
| 144:5  | `is_substantial_readme -> false` | CAUGHT | `readme_at_threshold_fires_but_one_below_does_not` (README exactly at threshold must fire) |
| 144:44 | `>= with <` (threshold boundary) | CAUGHT | `readme_at_threshold_fires_but_one_below_does_not` — asserts fire AT `README_SUBSTANTIAL_BYTES` and no-fire at `-1`; the exact-boundary example pins `>=` inclusivity |
| 161:5  | `detect_docs_present_and_substantial -> None` | CAUGHT | `readme_at_threshold_fires_but_one_below_does_not`; `docs_present_fires_only_on_the_readme_or_docs_disjunction` (fires when either disjunct holds) |
| 161:5  | `detect_docs_present_and_substantial -> Some(Default::default())` | UNVIABLE | `Signal` derives no `Default`, so the mutant does not compile — excluded from the denominator |

**Kill rate on the RGSD-4 detector: 4 / 4 viable mutants caught = 100%.**

## Verdict: PASS (100% ≥ 80% gate)

No surviving RGSD-4 mutant. The `>=` threshold boundary — the task's flagged
survivor spot — is already killed by the exact-boundary example test
(`fires(README_SUBSTANTIAL_BYTES)` ∧ `!fires(README_SUBSTANTIAL_BYTES - 1)`),
so a `>=`→`>` mutant (not even generated by cargo-mutants here) would also die:
at exactly 3000 bytes, `3000 > 3000` is false and the fire assertion fails.

The `||`→`&&` survivor the task anticipated does not exist on the RGSD-4 surface:
the disjunction is expressed as two guard-clause early-returns
(substantial-README branch, then docs-dir branch), not a boolean `||` operator,
and both disjunct paths (readme-only, docs-only) are exercised by
`docs_present_fires_only_on_the_readme_or_docs_disjunction`.

## Tests added

None — the RGSD-4 detector was already mutation-complete. No genuine gap; no
equivalent-mutant claim required (the only non-caught RGSD-4 mutant is unviable,
not surviving).
